This password generator will generate a semi-random password from an array of basic characters.  

After asking the users a  series of questions about the desired length and the type of characters to be included.  

If the user chooses a length that is out of scope, they wil be prompted to try again.  If they fail to select any character sets, a default set will be used.  

